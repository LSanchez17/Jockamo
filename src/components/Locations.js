

const Locations = () => {
    return(
        <div>
            <p>This pages holds their current locations
                Each location has a subset of the map, as delivery range
                Have form for user entering their address to see if they are
                within the range!
            </p>
        </div>
    );

}

export default Locations;